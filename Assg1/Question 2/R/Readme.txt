READMEs
Software used is R.

Pre-install the library SparseM and grid in R.
Then Read the files using SVMLight reader in R and write them back as text files.
For rest of the programming, use this script. It directly reads the images files.